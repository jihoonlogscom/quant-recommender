# 설치명 주의: import는 FinanceDataReader, PyPI 설치명은 finance-datareader(하이픈)
# pandas는 3.x에서 FDR/yfinance 호환 이슈로 <3 고정
finance-datareader>=0.9.95,<1
yfinance>=0.2.40
pandas>=2.1,<3
numpy>=1.26,<3
requests>=2.31
pykrx>=1.0          # 한국 수급(외국인·기관 순매수)
pyarrow>=14         # 일봉 증분 parquet 캐시

# 선택: 한국 재무(DART) — 환경변수 DART_API_KEY 필요
# OpenDartReader>=0.2.2

# 선택(Phase 3+ 정밀 검증·엔진): 필요 시 주석 해제
# alphalens-reloaded
# vectorbt
# quantstats
# purged-cross-validation
